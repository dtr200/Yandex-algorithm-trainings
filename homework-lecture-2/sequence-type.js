function getSequenceType(list){
    if(!list.length)
        return 

    let prev = list[0];

    for(let i = 1; i < list.length; i++){
        if(list[i] >= prev)
    }
}

getSequenceType([
    -530, -530, -530, -530, -530, -530
]);